export const lightTheme = {
    header: {
        bgColor: 'var(--darkestColor)',
        navigation: {
            linkColor: 'var(--grayColor)'
        }
    }
};

export const darkTheme = {
    header: {
        bgColor: 'var(--darkestColor)',
        navigation: {
            linkColor: 'var(--grayColor)'
        }
    }
};
